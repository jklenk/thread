# Claude Code Instructions

See **`AGENTS.md`** — it is the canonical tactical guide for all AI agents working on this project (including Claude Code). Everything you need is there: build & test commands, architecture, critical invariants, non-obvious gotchas, testing discipline, conventions, and the Beads session-close protocol.

For conceptual overview (what Thread does, what fidelity score means), read `README.md`.

Do not add project guidance to this file — keep it in `AGENTS.md` so every agent tool sees the same instructions.
